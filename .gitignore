node_modules/
dist/
.env
.env.*
!.env.example
cache/
log/
logs/
*.log
.DS_Store
test_data/

# Session Bus 运行时数据（多 session 协调，不提交）
.claude/sessions/

# 运行时用户数据（不提交）
data/workstyles/
packages/core/data/

# 产品内部规划文档
产品规划计划/

# SQLite 数据库及运行时文件
*.db
*.db-journal
*.db-shm
*.db-wal

# 运行时生成的数据目录（不提交数据，只提交结构）
data/bin/
data/repos/
data/content/
data/feedback/
data/archives/
data/backups/
# TLS 自签名证书（生产环境专用，不提交）
data/tls.key
data/tls.cert

# Tauri 编译产物
src-tauri/target/

# Tauri CLI 生成的移动端图标（不需要提交）
src-tauri/icons/android/
src-tauri/icons/ios/
src-tauri/icons/Square*.png
src-tauri/icons/StoreLogo.png

# ── 开源/闭源分离规则 ──────────────────────────────────────────────
# root public/  = FluxVita SPA（闭源，不同步到 GitHub）
# apps/jowork/public/ = JoWork SPA（开源，同步到 GitHub）
# 共享工具（styles/ theme.js i18n.js）可通过 fallback 机制共用，不含品牌信息
# 所有品牌改动：FluxVita → 改 public/xxx.html; JoWork → 改 apps/jowork/public/xxx.html
